# News Briefing Bot - Python Dependencies
#
# pyproject.toml is the source of truth for dependencies. This file exists for
# the conda/pip install paths; regenerate it after changing pyproject.toml with:
#   uv export --no-hashes --no-dev -o requirements.txt
#
# Install with: pip install -r requirements.txt
# Or with conda: conda install --file requirements.txt (for available packages)
# (uv users should run `uv sync` instead.)

# Core dependencies
pyyaml>=6.0
httpx>=0.27
python-dotenv>=1.0
lxml>=5.0

# Firecrawl for article extraction
firecrawl-py>=2.0

# OpenAI for LLM evaluation and summarization
openai>=1.30

# Dev tools (ruff, pytest, pytest-cov, mypy) live in pyproject.toml's
# [dependency-groups] dev — install with `uv sync` (includes dev by default).